{
  "id": "api",
  "cmd": "bundle install && PORT=$PORT bundle exec foreman start",
  "mem": 100,
  "cpus": 0.5,
  "instances": 1,
  "uris": [
    "https://github.com/felixroeser/demo-api/archive/v0.0.1.tar.gz"
  ],
  "env": {
    "RAILS_ENV": "production"
  }
}
