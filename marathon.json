{
  "id": "/api",
  "cmd": "bundle install && PORT=$PORT bundle exec foreman start",
  "mem": 100,
  "cpus": 0.5,
  "instances": 2,
  "uris": [
    "https://github.com/felixroeser/demo-api/tarball/master/"
  ],
  "env": {
    "RAILS_ENV": "production"
  },
  "ports": [
    0
  ]
}
